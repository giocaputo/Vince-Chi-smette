theme: jekyll-theme-cayman
title: "Vince chi smette -Contro la pratica dell'azzardo"
description: "Un progetto aperto per sensibilizzare contro l’azzardo e le sue conseguenze."
google_analytics: [G-9TFJY55K7F]
plugins:
  - jekyll-sitemap
defaults:
  - scope:
      path: "assets/css/style.css"
    values:
      sitemap: false

  - scope:
      path: "robots.txt"
    values:
      sitemap: false

  - scope:
      path: "sitemap.xml"
    values:
      sitemap: false
